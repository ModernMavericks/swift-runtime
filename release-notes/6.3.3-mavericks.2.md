ModernMavericks swift-runtime 6.3.3-mavericks.2.

- Repackage of libswiftCore for OS X 10.9 / x86_64, built from the same pinned
  swift-toolchain build environment. No source or patch change.
- Cut to exercise the shared `publish-release.yml`. This repo previously published
  with **no Release body at all** — its publish step set no `body_path`.
- ACCEPTANCE IS STILL MANUAL: a macOS 26 runner cannot exercise the 10.9-only
  behaviour this runtime exists to fix. Install on real 10.9.5 before trusting it.
