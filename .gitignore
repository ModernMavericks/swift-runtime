.build/
build/
out/
work/
dist/
*.pkg
*.tar.gz
# Never commit Apple SDK/toolchain bytes or built runtime binaries.
*.dylib
# macOS + tooling cruft (baseline parity with the other mavericks-* repos)
._*
.DS_Store
.superpowers/
docs/
/VERSION
/UPSTREAM_VERSION

# `build/` above covers -B build/updater, but the stdlib is configured `-B stdlib-build` and was
# covered by nothing.
stdlib-build/
